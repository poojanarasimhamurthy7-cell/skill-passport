{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Launch SkillPassport (Chrome)",
      "type": "chrome",
      "request": "launch",
      "url": "http://localhost:5174",
      "webRoot": "${workspaceFolder}/src"
    },
    {
      "name": "Launch SkillPassport (Edge)",
      "type": "msedge",
      "request": "launch",
      "url": "http://localhost:5174",
      "webRoot": "${workspaceFolder}/src"
    }
  ]
}
